[project]
name = "data-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dbt-core>=1.12.0",
    "dbt-postgres>=1.11.0",
    "psycopg2-binary>=2.9.12",
    "requests>=2.34.2",
]
